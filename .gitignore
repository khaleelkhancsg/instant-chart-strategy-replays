# Derived data cache — rebuild with `node prepare.mjs`.
# 54MB of typed arrays generated from a local CSV; no reason to version it.
data/*.bin
data/*.meta.json

node_modules/

# Local-only editor/tooling state
.claude/settings.local.json

# OS noise
Thumbs.db
.DS_Store

# Live bot: credentials, session logs and runtime state
bot/.env
bot/logs/
bot/logs_test/
bot/*_state.json
*_state.json
__pycache__/
*.pyc
